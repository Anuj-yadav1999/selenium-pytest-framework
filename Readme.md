# 🚀 Selenium + PyTest UI Automation Framework

## 📌 Project Overview

This project is a scalable UI automation framework built using **Selenium WebDriver** and **PyTest**, following the **Page Object Model (POM)** design pattern.

The framework is designed to simulate real-world enterprise automation architecture with reusable components, modular structure, and clean test separation.

---

## 🏗 Architecture & Design

The framework follows:

- ✅ Page Object Model (POM)
- ✅ Reusable Driver Factory
- ✅ PyTest Fixtures for setup & teardown
- ✅ Centralized locators
- ✅ Modular structure for scalability

